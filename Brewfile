# Packages these dotfiles depend on — not a full dump of installed apps.
# Applied automatically by chezmoi (run_onchange_after_brew-bundle.sh.tmpl).
brew "chezmoi"
brew "atuin"                   # ctrl+r history search (common.zsh)
brew "direnv"                  # common.zsh
brew "gh"                      # git credential helper (git/config)
brew "git-lfs"                 # git lfs filter (git/config)
brew "gnupg"                   # commit signing (git/config)
brew "pinentry-mac"            # gpg-agent passphrase prompt (git/config)
brew "jq"                      # json alias (aliases.zsh)
brew "sk"                      # skim fuzzy finder (aliases.zsh, functions.zsh)
brew "starship"                # prompt (starship.zsh)
brew "zoxide"                  # j command (common.zsh)
brew "zsh-syntax-highlighting" # common.zsh
